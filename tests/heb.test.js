// const checkHeb = require('../crawlers/heb');

describe('should fetch HEB data', () => {
  // TODO: Write actual tests
  it('should send slack message when vaccines are available', () => {
    expect('poop').toBe('poop');
  });

  it('should not send slack message when vaccines are not available', () => {
    expect('poop').toBe('poop');
  });
});
